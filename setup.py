from setuptools import setup, find_packages

setup(
    name='aorsa_cql_mcgo_toolkit',  
    version='0.1',
    description="RF code tools pre and post processing tools for editing profiles, visualizing output, iterations amongst them",
    packages=find_packages(),
)